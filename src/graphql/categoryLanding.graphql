query CategoryLanding($urlPath: String!) {
  categories(filters: { url_path: { eq: $urlPath } }) {
    items {
      id
      name
      url_key
      meta_title
      meta_description
      description
      children {
        uid
        name
        url_key
        url_path
        product_count
      }
      products(pageSize: 10) {
        items {
          uid
          name
          url_key
          image {
            url
            label
          }
          price_range {
            minimum_price {
              regular_price {
                value
                currency
              }
              final_price {
                value
                currency
              }
              discount {
                percent_off
              }
            }
          }
        }
      }
    }
  }
}
